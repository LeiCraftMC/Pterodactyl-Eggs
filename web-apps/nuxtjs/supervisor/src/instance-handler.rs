use once_cell::sync::Lazy;
use std::sync::RwLock;

#[derive(Debug)]
struct AppState {
    currentMainInstance: 

}

static STATE: Lazy<RwLock<AppState>> = Lazy::new(|| {
    
});

pub impl InstanceHandler {

    pub fn startup() {
        
    }

}
